pipeline {
        agent any
    
    options {
        timeout(time: 20, unit: 'MINUTES') 
    }
    

    stages {
        stage('List files'){
            steps {
                sh 'ls'
                sh 'pwd
            }
        }

    

    }
}
